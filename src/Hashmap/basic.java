package Hashmap;
import java.util.*;
public class basic {
    public static void main(String[] args) {
        HashMap<String,Integer> clg= new HashMap<>();
        // this creates a table of student names and their ranks
        clg.put("umesh",1); // to insert we use put
        clg.put("cootie",1);
        // clg.remove("umesh");
        // the key value must be unique and there is no constraints on value
        System.out.println(clg.get("cootie"));
        System.out.println(clg.containsKey("umesh")); // returns boolean
        System.out.println(clg);
        hm.conta
        //returns value of the key
    }
}
